[package]
name = "skl"
version = "0.1.0"
edition = "2024"
description = "AI skills package manager — install, share and manage skills, contexts and agents across your team."


[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
dialoguer = "0.12.0"
dirs = "6.0.0"
serde = { version = "1.0.228", features = ["derive"] }
toml = "1.0.7"
